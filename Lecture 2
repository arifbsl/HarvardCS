TOOLS

"help50" = in the terminal window along with program name will decode error messages

"style50' = will show how to make file stylistically correct

"CTRL + L = clear the terminal window

ARRAY

-Contiguous chunk of memory == the memory is back to back next to each other